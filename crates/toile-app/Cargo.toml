[package]
name = "toile-app"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

# Regla arquitectónica dura: toile-app solo importa toile-engine.
# Nunca agregar toile-sim / toile-mesh / toile-doc aquí — todo pasa por la
# API headless del engine (docs/architecture.html §2.6).
[dependencies]
eframe = "0.36.1"
toile-engine = { path = "../toile-engine" }
